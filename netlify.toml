[build]
  base = "frontend"
  publish = "."

# Set API_BASE_URL in the Netlify site's environment variables to the HTTPS
# Render service URL. Requests stay same-origin in the browser and are proxied.
[[redirects]]
  from = "/api/*"
  to = "$API_BASE_URL/api/:splat"
  status = 200
  force = true
